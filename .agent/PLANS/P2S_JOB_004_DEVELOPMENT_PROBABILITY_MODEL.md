# Codex Job Plan

## Job metadata
- Job ID: `P2S_JOB_004_DEVELOPMENT_PROBABILITY_MODEL`
- Status: COMPLETE_JOB004_PASS

## Frozen inputs
- `MODEL_EVALUATION_FREEZE_V1.json` and Markdown companion.
- `PRIMARY_GBDT_SEARCH_GRID_V1.json`.
- Job003 canonical dataset manifests and immutable reference-history SQLite DB.

## Resume sequence
1. Verify the Amendment 001 effective-rank authority, Job003B v1.1 data, and Job004R1 frozen runtime.
2. Preserve prior Job004 blocked pre-fit outputs through a SHA-256 inventory.
3. Verify frozen B0/Primary feature hashes, actual-starter universe, Job003B semantic audits, and the Kawasaki composition check.
4. Verify Amendments 002 (race-head inputs), 003 (CatBoost roles), 004 (EB residual/keys), and 005 (canonical DB path) before any fit.
5. Begin the fixed long Job004 execution only after every pre-fit authority passes.

## Amendment 006 execution

- Implement the four-layer Gauss-Seidel reference backfit with zero initialization, immediate interaction centering, and reestimate/fixed-component modes.
- Validate the reference algorithm on synthetic fixtures before opening project targets for fitting.
- Store the resumed run under `attempt_training_003`; preserve earlier attempts.
- Checkpoint model fits and derived predictions so an interrupted long run can resume without changing frozen semantics.

## Hard stops
- Do not substitute model libraries, parameters, optimizer, target-rank handling, evaluation gates, or scientific definitions.
- Any target-integrity or Job003B semantic failure blocks fitting.
- A missing race-head input definition or another frozen-spec implementation gap blocks fitting; no substitute model input may be inferred.

## Preflight result
- Amendments 001--006, the exact runtime, Job003B v1.1 universe, model-input roles, effective-rank target, and EB semantic preflights passed.
- Attempt `attempt_training_003` completed B0/Primary temporal raw fits, frozen candidate selection, and Amendment 006 EB replay.
- The frozen structural-surprisal target is undefined for the two accepted races with exactly three actual starters because `log(C(3,3)) = 0`.
- Race-head fitting and all downstream PL/evaluation phases remain stopped pending an explicit Research Lead authority for `n_r = 3`.

## Amendment 007 resume

- Preserve `attempt_training_003` and start `attempt_training_004`.
- Verify the two `n_r=3` races receive null `U_r`, no race-head/R1/R2 eligibility, `z_upset=0`, and M0 temperature semantics.
- Retain those races in ordinary B0/Primary, S2, WIDE, and floor evaluation.
- Reuse earlier fit/selection/EB checkpoints only after path, authority, and SHA-256 inventory checks.

## Completion

- Amendment 007 preflight passed for exactly two `n_r=3` races.
- `attempt_training_004` reused the audited attempt 003 raw-fit/selection/EB checkpoints read-only and completed race-head, PL, outer-valid probability generation, R1/R2/S2, bootstrap, and probability-floor evaluation.
- Final status: `JOB004_PASS`; no market/network/betting access and no PL/leakage integrity violations.
