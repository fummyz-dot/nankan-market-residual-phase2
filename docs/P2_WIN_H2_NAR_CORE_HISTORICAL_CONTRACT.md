# P2 WIN H2 NAR Core Historical Contract

P2-M10 is the registered H2 historical development evaluation. Its evidence is
`HISTORICAL_MARKET_TIME_UNKNOWN`, `DEVELOPMENT_REFERENCE_ONLY`, and
`H2_EVIDENCE_NOT_FRESH_HOLDOUT`: H1 performance, the outcomes, and the Market
baseline were already known before H2 began. It cannot establish a Phase 2
probability edge, T-15 equivalence, or an actual Primary gamma.

Formal real-data execution requires `P2_FORMAL_M10_EVALUATION=1`. It uses
exactly M09's frozen H1-C06 backend parameters, objective, folds, zero-tree
rule, and per-fold gamma values. H1 remains closed at six configurations and
is not retrained. `P2-INC-001` remains lineage only: it affected a pre-formal
H1 inner probe, did not contaminate M09 outer validation, and is not an H2
incidental peek.

Exactly four H2 candidates are registered: FS01 (Speed), FS02 (Speed+Pace),
FS03 (plus Class Rule), and FS04 (plus Class Empirical and Uncertainty). FS04
is the pre-designated NAR-core development candidate; C01–C03 are ablations
and cannot become Primary by post-hoc comparison. Each candidate runs once in
WF1–WF3. Its only tree features are the exact manifest columns for that set;
Market q/log-q are offsets only. P2_CURRENT, P2_BIAS, external/Keibabook,
P2_XVENUE, body weight, payout, ROI, feature importance selection, clipping,
and recalibration are prohibited.

The primary H2 status uses FS04's pooled race-equal candidate-minus-calibrated
Market loss. Sequential FS00→FS04 deltas are diagnostics only. The only H2
bootstrap is post-evaluation, race-date-block, 10,000 replicate FS04 versus
Market diagnostic with seed 20260818. The new-feature budget is 4/6 after M10;
H2-C05 is prospective P2_CURRENT and H2-C06 is unallocated, not automatically
usable.

## Registered M10 result (2026-08-19)

The four registered candidates completed once under the frozen protocol.  FS04
was retained as the pre-designated NAR-core candidate, without selecting among
the partial ablations.  Its pooled race-equal candidate-minus-calibrated-Market
loss was `+0.0023155723053499225`; therefore the development-only conclusion is
`H2_NAR_CORE_HISTORICAL_NO_SIGNAL`.  The FS04-versus-FS00 diagnostic delta was
`+0.0014746615270376535`.  This outcome does not alter H1, promote or remove
any block, confirm a probability edge, freeze T-15, or freeze the actual
Primary gamma.  The H2 budget is now `4/6`: H2-C05 remains prospective
P2_CURRENT and H2-C06 remains unallocated.
